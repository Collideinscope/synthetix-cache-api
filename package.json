{
  "name": "synthetix-cache-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "push-prod": "git push origin master && git push heroku master",
    "migrate": "NODE_ENV=development knex migrate:latest",
    "rollback": "NODE_ENV=development knex migrate:rollback",
    "seed": "NODE_ENV=development knex seed:run",
    "reset": "npm run rollback --all && npm run migrate && npm run seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "pg": "^8.11.5"
  }
}
